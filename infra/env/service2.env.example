# 두 번째 공개 서비스 env 템플릿.
#   infra/env/service2.env (600) 로 복사해서 쓴다.
#   서비스마다 시크릿을 따로 둔다 — 한 서비스가 뚫려도 다른 키를 못 읽게.
#
# 서비스가 정해지면 이 파일에 필요한 값을 채우고,
# infra/compose/docker-compose.yml 의 service2 주석을 푼다.

PORT=8788
# 예: WEB_DB=/app/data/service2.db
# 예: <SERVICE2_API_KEY>=
